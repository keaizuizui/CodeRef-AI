# CodeRef-AI - 智能代码分析引擎
# 依赖清单（v4.9.2 收敛：移除 pandas/tqdm/pathspec/ollama 四个死依赖）

# LLM集成（openai 客户端走官方 SDK；Ollama 走本地 HTTP API，无需 python ollama 包）
openai>=1.10.0

# 数据/网络
requests>=2.31.0        # Ollama Embedding / SCA OSV 在线查询等 HTTP 调用
numpy>=1.26.0           # 知识图谱向量化检索（code_knowledge_base：np.dot / np.linalg 相似度）
pyyaml>=6.0.1

# 日志
loguru>=0.7.2

# SCA 版本比较（未安装时 sca_checker 自动降级为字符串前缀比较，不影响其它功能）
packaging>=23.0

# ─── 可选依赖（非必需）────────────────────────────
# tree-sitter 中文解析为可选能力：核心解析走 Python 标准库 ast.parse，无需第三方。
# 未安装时自动降级，不影响任何分析功能（code_analyzer 内 try/except 容错）。
# 如需启用多语言 tree-sitter 解析，自行安装：
#   pip install tree-sitter==0.20.4 tree-sitter-languages>=1.10.0
# 注意：0.20.x 仅覆盖 Python 3.10-3.12；Python 3.13+ 无预编译 wheel，需编译工具链。
# tree-sitter==0.20.4
# tree-sitter-languages>=1.10.0