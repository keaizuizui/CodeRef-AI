# 开发响应册 · 开发方唯一写

> **owner**：开发方唯一写（编程 AI）。测试只读本册。
> **用途**：承接 `测试归档\课题台账\登记册.md` 中的课题，在此回报修复结果。
> **铁律**：只在本册写、不在测试侧登记册写；每响应一条课题，同时升版本 + 同步 `coderef-src` + 重启 MCP。
> 更新：2026-08-25 · 当前版本 v5.4.4

## 状态图例
- ⏳ 已认领
- 🔧 修复中
- ✅ 已修复（附版本）

## 承接回报记录

| 课题编号 | 主题摘录 | 承接状态 | 修复版本 | 回报日期 | 说明/证据指针 |
|---|---|---|---|---|---|
|  | review 占位率（可选） | ✅ 已修复 | v5.4.3 | 2026-08-25 | system prompt 更强制（明确"输出会被程序直接解析、违反即失败、无问题输出 []"）+ 重试 prompt 更严格（只输出 JSON 数组、禁 Markdown 标记）。零额外 LLM 调用、零额外耗时（对照 ）。冒烟：首调直接命中 JSON 未触发重试。证据：`core/code_review.py` `_call_llm` 两处 prompt；待测试回归确认占位率。 |
|  | 自由画布 分层布局/默认视图 Y 轴坍缩 | ✅ 已修复 | v5.4.4 | 2026-08-25 | 根因：节点 layer 趋同（type 均 default/module），`_layout_layered` 把全部节点归同一层 → 只 X 分布、Y 全相等 → 一条水平线；「适应」贴合坏线。修复：按依赖 DAG 最长链深度（Kahn 拓扑）给单层/超宽节点排序并拆子行（Python `_node_depths`+`_split_layer_rows` 与 JS `layoutLayered` 同步），Y 维逐行展开；`fitView` 加「适应」后节点最小可辨识尺寸下限（24px），防超大图缩成不可辨。验证：84 节点同层 1 行 → 拆 7 行，Y 展开 [230,1490]；力导向对照不变。证据：`core/canvas_engine.py`；待测试浏览器实测回归（默认视图/适应/分层布局三态均应二维可读）。 |

## 排期提示（开发方侧自留）
> - 承接前，先读登记册完整期望；处理完在 `__init__.py` + README 只升末位 patch。
> - 处理完记得：`copy` 同步改动文件到 `Coderef-Test\coderef-src\`，并重启 TRAE 的 coderef MCP（stdio 长驻进程），否则测试回归命中旧进程误判。
> - 修复语义按「工程约定」与 CodeRabbit 复审把关。